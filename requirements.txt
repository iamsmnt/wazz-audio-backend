# Wazz Audio Backend Requirements

# Note: wazz-audio-shared is installed separately in the Dockerfile

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Task queue
celery==5.3.4

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Pin to 4.0.1 - has __about__ module required by passlib 1.7.4

# Email
aiosmtplib==2.0.2
email-validator>=2.0.0


# Note: Database, storage, and core dependencies are in wazz-audio-shared
# sqlalchemy, pydantic, psycopg2-binary, boto3 are installed via wazz-audio-shared
